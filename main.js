/*==================
[NodeJS] CLI Argument Parser (Module)
	Language:
		NodeJS/10.13.0
==================*/
/**
 * @const {string} version
 */
const version = "0.1.0";

module.exports = {
	parse: require("./parse.js"),
	v: version,
	ver: version,
	version: version
};
